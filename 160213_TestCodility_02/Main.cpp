#include <stdlib.h>
#include <time.h>

#include "MaxCounters.h"
#include "MinAvgTwoSlice.h"
#include "Distinct.h"

int main()
{

	/*int N, M;
	int A[] = { 3, 4, 4, 6, 1, 4, 4 };
	N = 5;
	M = 7;
	MaxCounters counters;
	counters.solution(N, A, M);*/

	//srand((unsigned)time(NULL));

	////int N = 2 + rand() % 9;
	//int N = 7;
	//int *A = (int*)malloc(sizeof(int) * N);

	//MinAvgTwoSlice minAvg;

	//for (int i = 0; i < 10; ++i)
	//{
	//	for (int j = 0; j < N; ++j)
	//	{
	//		A[j] = (rand() % 20) - 10;
	//	}

	//	A[0] = -10;
	//	A[1] = -2;
	//	A[2] = -9;

	//	minAvg.solution(A, N);
	//}

	//srand((unsigned)time(NULL));

	//int N = 6;
	//int *A = (int*)malloc(sizeof(int) * N);
	//A[0] = 2; A[1] = 1; A[2] = 1; A[3] = 2; A[4] = 3; A[5] = 1;

	///*int N = 2;
	//int *A = (int*)malloc(sizeof(int) * N);
	//A[0] = 2; A[1] = 2;*/

	//Distinct distinct;

	//for (int i = 0; i < 10; ++i)
	//{
	//	/*for (int j = 0; j < N; ++j)
	//	{
	//		A[j] = (rand() % 20) - 10;
	//	}*/

	//	int numDistinct = distinct.solution(A, N);

	//	printf("%d\n", numDistinct);
	//}

	//return EXIT_SUCCESS;


}
