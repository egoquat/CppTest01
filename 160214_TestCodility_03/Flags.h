#include <vector>

class Flags
{
public:

	typedef unsigned int uint;
	int solution(vector<int> &A)
	{
		/*struct Pick
		{
			uint pick;
			uint flag;
		};*/

		if (A.size() < 2) return 0;

		uint cntFlags = 0;

		vector<uint> picks;
		vector<uint> flags;
		
		
		return min(picks.size(), flags.size());
	}
};